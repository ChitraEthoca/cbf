package webform;
/*package tests.WebForm;

import java.io.IOException;

import org.apache.log4j.Logger;
import org.openqa.selenium.WebDriver;
import org.testng.annotations.AfterMethod;
import org.testng.annotations.BeforeMethod;
import org.testng.annotations.Listeners;
import org.testng.annotations.Test;
import org.testng.asserts.SoftAssert;

import BaseClasses.Driver;
import pages.WebForm.S1_CBP_AdminConfiguresWIDescriptions;
import steps.ExternalSingleRequestFormSteps;
import steps.LoginSteps;

@Listeners(utilities.TestListener.class)
public class S1_CBP_AdminConfiguresWIDescriptions_Test {

	WebDriver driver = null;
	Driver driverObj = new Driver();
	ExternalSingleRequestFormSteps ESRFStepObj = new ExternalSingleRequestFormSteps();
	LoginSteps loginStepsObj = new LoginSteps();
	SoftAssert softAssert = null;
	Logger logger;

	@BeforeMethod(alwaysRun = true)
	private void startTest() throws IOException {
		driver = driverObj.createDriver();
		//driver.get(driverObj.getUrl());
		driver.manage().window().maximize();
		//loginStepsObj.login(driver, driverObj.getUsername(), driverObj.getPassword());
	}

	@Test(priority = 1, description = "S1_CBP_AdminConfiguresWIDescriptions_Test")
	public void bC_HetchHetchy_Single() throws Exception {
		
		S1_CBP_AdminConfiguresWIDescriptions objAdminConfig= new S1_CBP_AdminConfiguresWIDescriptions();
		
		objAdminConfig.s1_CBP_AdminConfiguresWIDescriptions();
		
	}
		
	@AfterMethod(alwaysRun = true)
	public void tearDown() throws InterruptedException {
		Thread.sleep(3000);
		//driver.quit();
	}
}*/